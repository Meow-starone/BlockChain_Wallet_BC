#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_rlp@2.1.0@rlp/bin/rlp" "$@"
  ret=$?
else 
  node  "$basedir/../../../_rlp@2.1.0@rlp/bin/rlp" "$@"
  ret=$?
fi
exit $ret
